<%- include('navbar.ejs') %>
    

            <div class="container mt-5">
    <div class="card shadow">
        <div class="card-header">
            <i class="fa-solid fa-file-pen me-2"></i> Edit Privacy Policy Section
        </div>

        <div class="card-body">
            <form method="POST" action="/admin/update_report/<%= report.policy_id %>">

                <div class="mb-3">
                    <label class="form-label">Section Number</label>
                    <input 
                        type="number" 
                        name="section_no" 
                        class="form-control"
                        value="<%= report.section_no %>" 
                        required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Section Title</label>
                    <input 
                        type="text" 
                        name="section_title" 
                        class="form-control"
                        value="<%= report.section_title %>" 
                        required>
                </div>

                <div class="mb-4">
                    <label class="form-label">Section Content</label>
                    <textarea 
                        name="section_content" 
                        class="form-control" 
                        rows="6" 
                        required><%= report.section_content %></textarea>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="fa-solid fa-check me-1"></i> Update
                    </button>
                    <a href="/admin/reports" class="btn btn-secondary">
                        <i class="fa-solid fa-arrow-left me-1"></i> Back
                    </a>
                </div>

            </form>
        </div>
    </div>
</div>

<%- include('footer.ejs') %>