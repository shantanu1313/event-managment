<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Admin | Service Slider Management</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #F4F6F9;
            color: #212529;
        }

        /* Page Header */
        .page-header {
            background: linear-gradient(135deg, #0B5ED7, #3BC9DB);
            color: #fff;
            padding: 22px 26px;
            border-radius: 16px;
            margin-bottom: 35px;
            display: flex;
            align-items: center;
            gap: 18px;
            box-shadow: 0 8px 22px rgba(11, 94, 215, 0.3);
        }

        .page-header i {
            font-size: 32px;
        }

        .page-header h3 {
            margin: 0;
            font-weight: 600;
        }

        .page-header p {
            margin: 4px 0 0;
            font-size: 14px;
            opacity: 0.9;
        }

        /* Cards */
        .service-card {
            background: #FFFFFF;
            border-radius: 16px;
            border: 1px solid #DEE2E6;
            box-shadow: 0 10px 26px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        /* Card Header */
        .card-header {
            background: linear-gradient(135deg, #0B5ED7, #3BC9DB);
            color: #fff;
            padding: 16px;
            font-weight: 600;
            height: 40px;
        }

        /* Form */
        .form-label {
            font-weight: 600;
            color: #212529;
        }

        .input-group-text {
            background: #E9F2FF;
            color: #0B5ED7;
            border: 1px solid #DEE2E6;
            border-right: 0;
        }

        .form-control {
            border: 1px solid #DEE2E6;
            border-left: 0;
            padding: 10px;
        }

        .form-control:focus {
            box-shadow: none;
            border-color: #0B5ED7;
        }

        /* Button */
        .btn-submit {
            padding: 12px 38px;
            border-radius: 12px;
            font-weight: 600;
            background: linear-gradient(135deg, #0B5ED7, #3BC9DB);
            color: #fff;
            border: none;
            transition: 0.3s ease;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(11, 94, 215, 0.4);
        }

        /* Table */
        .table thead {
            background: #0B5ED7;
            color: #fff;
        }

        /* Force table head text to white */
        .table thead th {
            color: #ffffff !important;
        }


        .table th,
        .table td {
            vertical-align: middle;
            padding: 14px;
        }

        .table tbody tr:hover {
            background-color: #F1F7FF;
        }

        .table img {
            width: 100px;
            border-radius: 12px;
            border: 1px solid #DEE2E6;
        }

        .btn-primary {
            background-color: #0B5ED7;
            border: none;
        }

        .btn-danger {
            background-color: #DC3545;
            border: none;
        }
    </style>
</head>

<body>

    <%- include('../navbar.ejs') %>

        <div class="container mt-4">

            <!-- 🔷 PAGE HEADER -->
            <div class="page-header">
                <i class="fas fa-sliders-h"></i>
                <div>
                    <h3>Service Slider Management</h3>
                    <p>Admin Panel → Services → Homepage Slider</p>
                </div>
            </div>

            <!-- 🔷 ADD SLIDER FORM -->
            <div class="row justify-content-center">
                <div class="col-md-7">
                    <div class="service-card">
                        <div class="card-header text-center text-white">
                            <i class="fas fa-plus-circle me-2"></i> Add New Service Slider
                        </div>
                        <div class="card-body p-4">
                            <form action="/admin/service/save_service_slider" method="post"
                                enctype="multipart/form-data">

                                <div class="mb-3">
                                    <label class="form-label">Title</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-heading"></i></span>
                                        <input type="text" name="title" class="form-control" required>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label">Description</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-align-left"></i></span>
                                        <textarea name="description" rows="3" class="form-control" required></textarea>
                                    </div>
                                </div>

                                <div class="mb-4">
                                    <label class="form-label">Image</label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-image"></i></span>
                                        <input type="file" name="image" class="form-control" required>
                                    </div>
                                </div>

                                <div class="text-center text-white">
                                    <button type="submit" class="btn btn-submit text-white">
                                        <i class="fas fa-save me-1"></i> Save Slider
                                    </button>
                                </div>

                            </form>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 🔷 SLIDER LIST -->
            <div class="row mt-5">
                <div class="col-12">
                    <div class="service-card">
                        <div class="card-header text-white">
                            <i class="fas fa-list me-2 text-white"></i> Existing Service Sliders
                        </div>
                        <div class="card-body p-0">
                            <table class="table table-striped table-hover mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Image</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if(data && data.length> 0){ %>
                                        <% for(let i=0; i < data.length; i++){ %>
                                            <tr>
                                                <td>
                                                    <%= i + 1 %>
                                                </td>
                                                <td>
                                                    <img src="/upload/service/<%= data[i].image %>" alt="Slider Image">
                                                </td>
                                                <td>
                                                    <%= data[i].title %>
                                                </td>
                                                <td>
                                                    <%= data[i].description %>
                                                </td>
                                                <td>
                                                    <a href="/admin/service/edit_service_slider/<%= data[i].slider_id %>"
                                                        class="btn btn-sm btn-primary me-1">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <a href="/admin/service/delete_service_slider/<%= data[i].slider_id %>"
                                                        class="btn btn-sm btn-danger"
                                                        onclick="return confirm('Are you sure you want to delete this slider?')">
                                                        <i class="fas fa-trash"></i>
                                                    </a>
                                                </td>
                                            </tr>
                                            <% } %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="5" class="text-center py-4">No service sliders
                                                            found.</td>
                                                    </tr>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>

        <%- include('../footer.ejs') %>

</body>

</html>