<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Contact Info | Admin</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f9;
        }
        .card-header {
            font-weight: 600;
        }
    </style>
</head>
<body>

<div class="container mt-4">

    <!-- PAGE TITLE -->
    <h3 class="mb-4">✏️ Edit Contact Info</h3>

    <!-- EDIT CONTACT CARD -->
    <div class="card">
        <div class="card-header bg-primary text-dark">
            Update Contact Details
        </div>

        <div class="card-body">
            <form action="/admin/update-contact/<%= contact.contact_id %>" method="POST">

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Address</label>
                        <textarea name="contact_address" class="form-control" required><%= contact.contact_address %></textarea>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Phone</label>
                        <input type="text" 
                               name="contact_phone" 
                               class="form-control"
                               value="<%= contact.contact_phone %>" 
                               required>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Email</label>
                        <input type="email" 
                               name="contact_email" 
                               class="form-control"
                               value="<%= contact.contact_email %>" 
                               required>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Availability</label>
                        <input type="text" 
                               name="contact_availability" 
                               class="form-control"
                               value="<%= contact.contact_availability %>">
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Response Time</label>
                        <input type="text" 
                               name="contact_response_time" 
                               class="form-control"
                               value="<%= contact.contact_response_time %>">
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button class="btn btn-success">
                        💾 Update
                    </button>

                    <a href="/admin/privacy-logs" class="btn btn-secondary">
                        ↩ Back
                    </a>
                </div>

            </form>
        </div>
    </div>

</div>

</body>
</html>
